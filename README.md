# Project Talk Images
Drop new images into `/incoming`. A workflow will:
- optimize & resize them,
- write outputs to `/images/full` and `/images/thumbs`,
- update `/images/index.json` for easy linking.

Use links like:
`https://cdn.jsdelivr.net/gh/ORG/project-talk-images@main/images/full/FILE`
